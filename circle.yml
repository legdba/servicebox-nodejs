machine:
  node:
    version: 4.3.2
  services:
    - docker
deployment:
  production:
    branch: master
    commands:
      - ./build_docker.sh
  staging:
    branch: /.*/
    commands:
      - ./build_docker.sh
